# Taste
- Uses uv as the Python package/dependency manager (`uv sync --frozen`, `uv run python manage.py ...`), with `pyproject.toml` + `uv.lock` rather than pip/poetry/requirements.txt. Confidence: 0.8
- Uses Docker Compose for local development, with a dev-specific compose file (`docker-compose.dev.yml`) and a thin `start.sh` wrapper that runs `docker compose ... up --build`. Confidence: 0.7
- Runs Django's dev server (`runserver 0.0.0.0:8000`) in Docker for local work. Confidence: 0.6
- Prefers simple, minimal solutions — asked for the Dockerfile, compose file, and start script to be written "simply", and for Swagger UI to be added "in simplest way" (just URL wiring since drf-spectacular was already configured — no extra packages or boilerplate). Confidence: 0.7
- Prefers Django REST Framework's class-based `APIView` for API endpoints (explicitly requested the OCR endpoint be built this way). Confidence: 0.7
- Prefers API endpoints to accept input via JSON request body and return the result as a JSON response. Confidence: 0.5
- Prefers to review and approve a written plan (plan mode) before files are created or edited — approved a plan for the Postgres integration and then let it be implemented as written. Confidence: 0.8
- Expects API endpoints to be fully documented in Swagger UI / OpenAPI schema, including the JSON request body — noticed the missing request area for a plain `APIView` and asked for the drf-spectacular `@extend_schema` decorator with a request serializer to expose it. Confidence: 0.6
hould be set up (e.g., supplied a docker-compose file + settings.py from a previous project when asked to integrate a database). Confidence: 0.6
- Wires Docker Compose database services with a Postgres pattern reused from another project: `pg_isready` healthcheck, `depends_on: condition: service_healthy` on the web service, a named volume for data, and DB credentials injected from `.env` via `env_file`. Confidence: 0.7
- Keeps secrets and configuration in a gitignored `.env` file (compose `env_file` + `os.environ.get` with sensible defaults in settings.py) rather than hardcoding credentials in source — accepted this over the hardcoded values from their reference snippet. Confidence: 0.6
- Prefers tightly scoped work: explicitly asked to "only work on this part" when requesting a new feature (schema-storage endpoint), i.e., no scope creep into unrelated areas. Confidence: 0.7
- Prefers storing JSON payloads verbatim ("as it is") in a single JSON column (JSONField → jsonb) rather than normalizing them into separate relational fields/columns. Confidence: 0.6
- Treats the DB-stored schema JSON as the source of truth for model definitions: wants to fetch the latest stored `schema_data` row and use it to dynamically build Pydantic model fields, e.g. `client_code: ConfidenceScore = Field(description=data["applicant_info"]["fields"]["client_code"]["description"])`, rather than hardcoding field descriptions. Restated this explicitly with the full schema JSON payload (sections like `applicant_info`/`bank_accounts` carrying a `description` and a `fields` dict of per-field `description` strings). Confidence: 0.8
- Prefers to specify the exact code shape they want in the request (e.g., giving the precise line `client_code: ConfidenceScore = Field(description=data["applicant_info"]["fields"]["client_code"]["description"])`) rather than only describing the intent. Confidence: 0.5
- Prefers hardcoded, explicitly-written Pydantic schema classes over dynamic model generation via `create_model` — explicitly rejected `create_model` and asked for the schema "hard coded just like my previous schema", even when field descriptions come from the DB at runtime (accepted defining the classes inside a function that takes `data`). Confidence: 0.8
